# === JS/Frontend ===
node_modules/
# Keep only the lockfile of the package manager you use; comment out the rest.
# pnpm-lock.yaml
# package-lock.json
# yarn.lock

# Build output
dist/
.vite/

# === Python/Backend ===
__pycache__/
*.py[cod]
venv/
.venv/

# Local DB & bundles
db.sqlite3
papsas-backend.tar.gz
*.tar.gz
*.zip

# Temp/output from earlier steps
patches/
new_files/
repo_info.txt
structure.txt
versions.txt

# === Environment files ===
.env
.env.local
.env.*.local
*.env
# Do NOT ignore the example env
!.env.example

# === Server-only / build artifacts ===
staticfiles/
papsas/settings.py*.bak

# === Dev artifacts ===
.DS_Store
*.log
login.json
*.bak.*
*.bracefix.*
*.dupefix.*
*.finalfix.*
*.fix.*
